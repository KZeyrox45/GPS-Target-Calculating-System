# Lessons Learned — Running Log

Purpose: record every mistake the user points out, what was learned from it, and how future work changes to avoid repeating it. Updated after every task phase and every correction.

| # | Date | Mistake / Correction | Lesson | Prevention |
|---|------|---------------------|--------|------------|
| 1 | 2026-08 | Weekly reports were under the supervisor's minimum length; several weeks sat at 8-12 pages when 15+ was required (now raised to 18-25). | Page-count requirements must be verified against the *current* requirement, not an old one. Requirements can change mid-project (CBHD feedback). | After every report edit, recompile each week standalone and check page count against `IMPORTANT_NOTE.md` requirements before declaring done. |
| 2 | 2026-08 | Report content was judged "just theoretical" with no implementation evidence and too similar to phase 1. | A thesis report must demonstrate concrete implemented work, not repeat textbook theory. Phase 2 must be clearly distinct from phase 1. | Every week's content must tie back to actual implementation artifacts (verified test runs, real screenshots, real benchmark numbers) produced during that work. |
| 3 | 2026-08-23 | Two different RULES.md files existed (`RULES.md` root vs `.agents/rules/RULES.md`) with conflicting content (346 vs 230 lines), causing rule-source ambiguity. | Multiple rule sources guarantee confusion about which constraints apply. One canonical file only. | Single canonical copy at `RULES/RULES.md`; `.agents/rules/RULES.md` is a pointer stub. Never create new rule files elsewhere. |
| 4 | 2026-08 | Chinese characters (方位, 稳定) leaked into Vietnamese `.tex` files, producing broken output. | Mixed-language corruption happens during generation; charset hygiene is part of QA. | Grep all `.tex` for CJK ranges (`\x{4e00}-\x{9fff}`) as a standard pre-delivery check. |
| 5 | 2026-08 | AI-pattern phrases ("đã mô tả", "đáng chú ý", "cần lưu ý", "như đã dự kiến") appeared repeatedly across weekly reports. | Robotic/formulaic connectors are the most common AI fingerprint in Vietnamese academic prose. | Run anti-AI pattern scans (meta-phrases, inflated words, formulaic connectors) on every `.tex` edit batch. Full prohibited list lives in `RULES/RULES.md`. |
| 6 | 2026-08 | Initial page counts were measured with a wrong document wrapper, giving misleading numbers until corrected. | Measurement tooling must replicate the real build environment exactly. | Compile weekly tests with the identical preamble as `report-weekly/main.tex`; verify against `main.tex` full-document builds too. |
| 7 | 2026-08-23 | Proposed mission order II→I→III; user corrected to I→II→III. | Reports written *before* code verification contain unverified claims. Evidence-first ordering produces grounded content. | Complete project refinement (runtime + static audit, real test outputs, screenshots) before writing any report prose that cites results. |
| 8 | 2026-08-23 | Assumed `figures/` and `sim/` were root-level folders; they are actually subfolders inside `Images/`. | Folder-structure assumptions from prose descriptions must be confirmed by filesystem survey before acting. | Survey workspace layout first (as done); ask when note descriptions don't match observed reality. |
| 9 | 2026-08-23 | Static audit found the theme toggle was fully decorative (state changed, zero CSS consumed it) - exactly the class of bug the user warned about ("functional or just for decoration?"). | UI controls must be traced end-to-end (button -> state -> visible effect), not just checked for existence of a handler. | Runtime audit checklist: for every toggle/control, verify a visible state change occurs after clicking. |
| 10 | 2026-08-23 | Found `alpha_sensitivity.png` (listed as an official report figure) was generated from admittedly fabricated values, and a sibling figure script crashes on a deleted JSON dependency. | Figures in reports must have verifiable provenance: generated by scripts that run against real simulation data. Any "approximate values for visualization" comment is a red flag for academic integrity. | Every report figure gets regenerated from real runs during image verification; provenance (script + data source) recorded next to each figure. |
| 11 | 2026-08-23 | README documented a WebSocket path that never existed (`/ws/{id}` vs actual `/ws/tracking/{id}`); demo script listed wrong layer colors; limitations section described pre-fix reality. | Docs drift from code silently. Factual claims in docs need periodic re-verification against source. | When touching any subsystem, grep docs for claims about it (paths, colors, numbers) and re-verify. |
| 12 | 2026-08-23 | Editing the WS handler added a second `finally` block to the same try, breaking collection; caught immediately because tests ran right after the edit batch. | Small fix batches + immediate test runs catch integration mistakes within seconds. | Keep the rhythm: edit batch -> pytest/lint/build -> only then move to next batch. |
| 13 | 2026-08-23 | `alpha_sensitivity.png` official figure was built from hardcoded approximate values (comment: "approximate values for visualization"); `gen_report_figures.py` sibling crashed on a deleted truth-JSON dependency. | Every number in a published figure must trace to a script that runs against real simulation output. "Approximate for visualization" in a thesis figure is academic misconduct. | Ran a real alpha sweep (seed=42, 120s, 10Hz, 9 alpha values 0.1-0.9 via benchmark_rmse.run_scenario) — optimum at alpha=0.2 (RMSE 0.21m), not 0.1 as fabricated data claimed. |
| 14 | 2026-08-23 | Benchmarks measured old `MotorcycleTrajectory` (kinematic fallback) while the live system ships `RoadNetworkMotorcycleTrajectory` — published RMSE measured a model users never see. | Benchmark scope must match deployed configuration. Measuring a fallback while shipping a different model misrepresents system performance. | Added `motorcycle_road` scenario to both benchmark scripts (road-network: KF 1.52m vs kinematic KF 1.80m; Kalman actually improves on road-constrained motion). |
