\section{Tuần 1}

\subsection{Giới thiệu đề tài}

\hspace{0.5cm} Đồ án tốt nghiệp thuộc ngành Kỹ thuật Máy tính, thuộc chương trình Đào tạo Kỹ sư Khoa học Máy tính (CO4347), đề tài "Xây dựng thuật toán tính toán tọa độ mục tiêu từ dữ liệu GPS, góc ngắm và khoảng cách", với phần mở rộng là "Tracking mục tiêu di động thời gian thực với đa cảm biến Laser-IMU-GNSS".

Trong giai đoạn 1 (Phase 1), nhóm đã xây dựng thành công thuật toán tính toán tọa độ mục tiêu ở trạng thái tĩnh. Hệ thống nhận đầu vào là vị trí GPS của người quan sát, góc ngắm (azimuth, elevation) đo từ cảm biến quán tính (IMU), và khoảng cách đo bằng máy đo khoảng cách laser. Đầu ra là tọa độ địa lý (kinh độ, vĩ độ, cao độ) của mục tiêu. Thuật toán được chạy theo từng điểm riêng lẻ (single-point): mỗi lần tính toán là một lần đọc cảm biến riêng biệt, không có sự liên kết giữa các lần đo về mặt thời gian. Kết quả giai đoạn 1 đã được kiểm chứng bằng 104 test tự động và đánh giá trên giao diện bản đồ số.

Giai đoạn 2 (Phase 2) mở rộng bài toán đáng kể: mục tiêu không còn đứng yên mà có thể di chuyển với tốc độ và hướng thay đổi theo thời gian. Hệ thống phải tính toán liên tục, cập nhật vị trí mục tiêu theo thời gian thực và hiển thị quá trình di chuyển trên bản đồ số. Yêu cầu này đòi hỏi xử lý dòng dữ liệu cảm biến liên tục, kết hợp nhiều nguồn cảm biến và áp dụng các bộ lọc ước lượng (estimation filter) để xác định vị trí chính xác ngay cả khi tín hiệu bị nhiễu hoặc gián đoạn.

Một thay đổi quan trọng trong giai đoạn 2 là mô hình triển khai. Trong giai đoạn 1, thuật toán chạy trực tiếp trên trình duyệt web. Giai đoạn 2 chuyển sang mô hình máy chủ kết hợp trình duyệt: phía máy chủ xử lý thuật toán và quản lý trạng thái, phía giao diện hiển thị bản đồ và điều khiển. Kiến trúc này phản ánh thiết kế thực tế của hệ thống theo dõi, nơi thiết bị nhúng (Raspberry Pi) gửi dữ liệu cảm biến lên máy chủ trung tâm, máy chủ xử lý và trả kết quả về giao diện người dùng.

Bảng \ref{tab:phase-compare} so sánh chi tiết hai giai đoạn trên các phương diện kỹ thuật chính.

\begin{table}[H]
\centering
\caption{So sánh tổng quan giữa giai đoạn 1 và giai đoạn 2}
\label{tab:phase-compare}
\adjustbox{max width=\linewidth}{
\begin{tabular}{p{3.5cm}p{5cm}p{5cm}}
\toprule
\textbf{Phương diện} & \textbf{Giai đoạn 1} & \textbf{Giai đoạn 2} \\
\midrule
Mục tiêu & Tính tọa độ mục tiêu tĩnh & Theo dõi mục tiêu di chuyển liên tục \\
Dữ liệu đầu vào & Một phép đo duy nhất & Dòng dữ liệu liên tục 10 Hz \\
Thuật toán lọc & Không có & Kalman, alpha-beta \\
Nguồn cảm biến & Laser + IMU + GPS & Laser + IMU + GPS (hợp nhất) \\
Mô hình mục tiêu & Không có & Constant Velocity 2D/3D \\
Nền tảng chạy & Trình duyệt web & FastAPI (máy chủ) + React (giao diện) \\
Kết quả đầu ra & Một tọa độ duy nhất & Quỹ đạo liên tục trên bản đồ \\
Số test tự động & 104 & 162 \\
\bottomrule
\end{tabular}
}
\end{table}

\subsection{Phát biểu bài toán}

\hspace{0.5cm} Bài toán được phát biểu như sau: Cho một người quan sát mang thiết bị GNSS (GPS), cảm biến IMU, và máy đo khoảng cách laser. Người quan sát này hướng thiết bị về phía mục tiêu đang di chuyển. Tại mỗi thời điểm $k$, hệ thống cần thực hiện các nhiệm vụ sau:

\begin{enumerate}
    \item \textbf{Xác định vị trí người quan sát}: Lấy tọa độ tuyệt đối (kinh độ $\lambda$, vĩ độ $\phi$, cao độ $h$) từ thiết bị GNSS. GPS dân sự tiêu chuẩn có độ chính xác 2 đến 5 m ở điều kiện ngoài trời tốt.
    \item \textbf{Đo góc ngắm}: Lấy góc azimuth $\alpha$ (góc giữa hướng Bắc và hướng ngắm, tính theo chiều kim đồng hồ từ 0\textdegree{} đến 360\textdegree{}) và góc elevation $\beta$ (góc cao so với mặt phẳng ngang, từ $-90$\textdegree{} đến $+90$\textdegree{}) từ cảm biến IMU. Cảm biến quán tính có tần số cao (100 đến 1000 Hz) nhưng bị trôi (drift) theo thời gian.
    \item \textbf{Đo khoảng cách}: Lấy khoảng cách thực tế từ người quan sát đến mục tiêu bằng máy đo laser rangefinder. Độ chính xác cao ($\pm 1$ đến 2 cm) nhưng bị giới hạn bởi tầm xa và yêu cầu tầm nhìn thẳng (Line-of-Sight).
    \item \textbf{Tính toán tọa độ mục tiêu}: Từ ba đại lượng trên, tính toán tọa độ 3D (kinh độ, vĩ độ, cao độ) của mục tiêu trong hệ tọa độ WGS-84 thông qua chuỗi chuyển đổi WGS-84 $\rightarrow$ ECEF $\rightarrow$ ENU.
    \item \textbf{Lọc và ước lượng}: Áp dụng bộ lọc Kalman hoặc bộ lọc alpha-beta để ước lượng vị trí và vận tốc của mục tiêu, giảm ảnh hưởng của nhiễu cảm biến.
    \item \textbf{Hiển thị thời gian thực}: Truyền kết quả lên giao diện web qua kênh truyền dữ liệu hai chiều và hiển thị vị trí, quỹ đạo mục tiêu trên bản đồ số.
\end{enumerate}

Các ràng buộc chính của bài toán bao gồm:

\begin{itemize}
    \item \textbf{Thời gian thực}: Tổng thời gian xử lý một chu kỳ (từ nhận dữ liệu cảm biến đến hiển thị kết quả) phải nhỏ hơn 100 ms để đảm bảo tính cập nhật liên tục ở tần số 10 Hz.
    \item \textbf{Nhiễu cảm biến}: Dữ liệu từ GPS, IMU và laser đều bị ảnh hưởng bởi nhiễu môi trường và sai số chế tạo. Mô hình hóa nhiễu là bước tiền đề để thiết kế bộ lọc phù hợp.
    \item \textbf{Mất tín hiệu GPS}: Tín hiệu GPS có thể bị mất hoặc suy giảm trong nhà, khu vực che chắn hoặc dưới tán rừng. Hệ thống cần duy trì ước lượng trong thời gian mất tín hiệu.
    \item \textbf{Tầm nhìn thẳng}: Laser rangefinder chỉ hoạt động trong tầm nhìn thẳng, bị giới hạn bởi tầm xa của thiết bị (thường 40 đến 100 m tùy loại).
    \item \textbf{Đa loại mục tiêu}: Mục tiêu có thể là người đi bộ (tốc độ 1 đến 2 m/s), xe máy (tốc độ 5 đến 15 m/s) hoặc drone (tốc độ 5 đến 15 m/s, bay 3D). Mỗi loại có mô hình động học riêng.
    \item \textbf{Biên hoạt động}: Phạm vi theo dõi bị giới hạn bởi bán kính cảm biến hiệu dụng, thường trong khoảng 100 đến 1000 m.
\end{itemize}

\subsection{Tổng quan hệ thống Phase 2}

\hspace{0.5cm} Hệ thống được thiết kế theo kiến trúc phân lớp (layered architecture), gồm bốn tầng chính. Mỗi tầng đảm nhận một vai trò riêng biệt và giao tiếp với nhau qua các giao thức chuẩn. Kiến trúc này được thể hiện trong Hình \ref{fig:phase2-arch}.

\begin{figure}[H]
\centering
\adjustbox{max width=\linewidth}{
\begin{tikzpicture}[
    box/.style={
        rectangle, draw, rounded corners=3mm, 
        minimum width=4.5cm, minimum height=1.2cm, 
        align=center, font=\small\sffamily
    },
    labelbox/.style={
        font=\normalsize\sffamily, align=center, inner sep=4pt
    },
    arrow/.style={->, thick, >=stealth, shorten >=3pt, shorten <=3pt},
    every node/.style={font=\normalsize\sffamily}
]

% Layer 4: Frontend
\node[box, fill=blue!15] (frontend) at (0,5.5) {Tầng giao diện\\React + Leaflet + Vite};
\node[box, fill=blue!8] (browser) at (0,4.1) {Trình duyệt Web};

% Layer 3: Server
\node[box, fill=green!15] (server) at (0,2.2) {Tầng máy chủ\\FastAPI + WebSocket};
\node[box, fill=green!8] (api) at (0,0.8) {REST API + DataFrame Buffer};

% Layer 2: Embedded
\node[box, fill=yellow!15] (embedded) at (0,-1.2) {Tầng xử lý nhúng\\Raspberry Pi};
\node[box, fill=yellow!8] (firmware) at (0,-2.9) {Firmware đọc cảm biến};

% Layer 1: Sensors
\node[box, fill=red!15, minimum width=2.8cm] (gps) at (-4,-5.2) {GPS/GNSS};
\node[box, fill=red!15, minimum width=2.8cm] (imu) at (0,-5.2) {IMU};
\node[box, fill=red!15, minimum width=2.8cm] (laser) at (4,-5.2) {Laser};

% Arrows
\draw[arrow] (browser) -- node[midway, right=3pt, font=\footnotesize] {WebSocket} (server);
\draw[arrow] (api) -- node[midway, right=3pt, font=\footnotesize] {TCP/IP} (embedded);
\draw[arrow] (embedded) -- (firmware);
\draw[arrow] (firmware) -- node[midway, left=5pt, font=\footnotesize] {UART/I2C} (gps);
\draw[arrow] (firmware) -- node[midway, right=2pt, font=\footnotesize] {I2C} (imu);
\draw[arrow] (firmware) -- node[midway, right=5pt, font=\footnotesize] {UART} (laser);

% Labels
\node[labelbox, text=blue!50!black] at (6.8,4.8) {Frontend\\(client)};
\node[labelbox, text=green!50!black] at (6.8,1.45) {Backend\\(server)};
\node[labelbox, text=yellow!50!black] at (6.8,-1.7) {Embedded\\(field device)};
\node[labelbox, text=red!50!black] at (6.8,-4.7) {Sensors\\(hardware)};

\end{tikzpicture}
}
\caption{Kiến trúc phân lớp bốn tầng của hệ thống Phase 2}
\label{fig:phase2-arch}
\end{figure}

\subsubsection{Tầng cảm biến (Sensor Layer)}

\hspace{0.5cm} Đây là tầng thấp nhất, bao gồm phần cứng cảm biến đặt trên thiết bị di động. Ba loại cảm biến chính tạo thành hệ thống đo lường đa nguồn:

\begin{itemize}
    \item \textbf{Module GPS/GNSS}: Cung cấp tọa độ tuyệt đối (kinh độ, vĩ độ, cao độ) của người quan sát. Tần số cập nhật 1 đến 10 Hz. Độ chính xác 2 đến 5 m ở chế độ dân sự trong điều kiện ngoài trời tốt. Dữ liệu được xuất theo định dạng NMEA-0183 qua giao tiếp UART.
    \item \textbf{Cảm biến IMU} (Inertial Measurement Unit): Đo góc hướng azimuth, góc nghiêng elevation, roll và pitch. Cảm biến có tần số cao (100 đến 1000 Hz) nhưng bị trôi theo thời gian. Cần kết hợp với GPS để hiệu chỉnh. Thông thường sử dụng MPU6050, ICM-20948 hoặc BNO055 qua giao thức I2C/SPI.
    \item \textbf{Máy đo khoảng cách laser} (Laser rangefinder): Đo khoảng cách trực tiếp đến mục tiêu bằng phương pháp Time-of-Flight. Độ chính xác cao ($\pm 1$ đến 2 cm) nhưng bị giới hạn bởi tầm xa (40 đến 100 m tùy loại) và yêu cầu tầm nhìn thẳng (Line-of-Sight).
\end{itemize}

Trong điều kiện lý tưởng, mỗi cảm biến hoạt động độc lập và bổ sung cho nhau. Tuy nhiên, trong thực tế, tín hiệu từ mỗi cảm biến đều bị nhiễu: GPS có nhiễu multipath, IMU có drift nhiệt độ, laser có nhiễu phản xạ bề mặt. Mục tiêu của hợp nhất cảm biến (sensor fusion) là kết hợp ba nguồn thông tin này để giảm thiểu ảnh hưởng của nhiễu riêng từng cảm biến.

\subsubsection{Mô hình nhiễu cảm biến}

\hspace{0.5cm} Mô hình hóa nhiễu là bước tiền đề để thiết kế bộ lọc phù hợp. Mỗi loại cảm biến có đặc tính nhiễu riêng, được mô tả bằng các tham số sau:

\begin{table}[H]
\centering
\caption{Tham số mô hình nhiễu cảm biến và giá trị sử dụng}
\label{tab:noise-model}
\adjustbox{max width=\linewidth}{
\begin{tabular}{llcc}
\toprule
\textbf{Cảm biến} & \textbf{Tham số} & \textbf{Giá trị} & \textbf{Đơn vị} \\
\midrule
\multirow{2}{*}{GPS/GNSS} & $\sigma_{\text{gps}}$ (position) & 5,0 & m \\
& Tần số cập nhật & 10 & Hz \\
\midrule
\multirow{3}{*}{IMU} & $\sigma_{\text{az}}$ (azimuth) & 0,3 & độ \\
& $\sigma_{\text{el}}$ (elevation) & 0,2 & độ \\
& Tần số đọc & 100 & Hz \\
\midrule
\multirow{2}{*}{Laser} & $\sigma_{\text{range}}$ (khoảng cách) & 0,5 & m \\
& Tầm xa tối đa & 1000 & m \\
\bottomrule
\end{tabular}
}
\end{table}

Giá trị $\sigma_{\text{gps}} = 5{,}0$ m phản ánh độ chính xác trung bình của GPS dân sự (chế độ không PPP, không RTK) trong điều kiện ngoài trời tốt, nơi tín hiệu GNSS phản xạ từ các bề mặt kiến trúc (multipath) gây sai số position 2 đến 5 m. Giá trị $\sigma_{\text{az}} = 0{,}3^\circ$ và $\sigma_{\text{el}} = 0{,}2^\circ$ phản ánh sai số góc của IMU cấp tiêu dùng (MPU6050 hoặc tương đương), bị ảnh hưởng bởi rung động môi trường và sai số calibration. Giá trị $\sigma_{\text{range}} = 0{,}5$ m phản ánh sai số laser rangefinder ở khoảng cách trung bình (100 đến 500 m), bao gồm sai số thời gian bay và phản xạ bề mặt.

Mỗi loại mục tiêu có thể có tham số laser khác nhau tùy khoảng cách điển hình: người đi bộ ở khoảng 50 đến 200 m ($\sigma_{\text{range}} = 0{,}3$ m), xe máy ở khoảng 100 đến 500 m ($\sigma_{\text{range}} = 0{,}5$ m), drone ở khoảng 200 đến 800 m ($\sigma_{\text{range}} = 1{,}0$ m). Trong mô phỏng tiêu chuẩn, giá trị $\sigma_{\text{range}} = 0{,}5$ m được sử dụng cho cả ba loại mục tiêu để đảm bảo tính so sánh được.

\subsubsection{Tầng xử lý nhúng (Embedded Layer)}

\hspace{0.5cm} Raspberry Pi (RPi 3B+ hoặc 4B) đóng vai trò là bộ điều khiển trung tâm (central hub) kết nối các cảm biến. Phần mềm đọc cảm biến được viết bằng Python, chạy trên Raspberry Pi OS (Linux-based).

Vai trò chính của Raspberry Pi bao gồm: đọc dữ liệu từ GPS qua UART (giao tiếp serial), đọc dữ liệu từ IMU qua I2C, đọc dữ liệu từ laser rangefinder qua UART hoặc I2C, đóng gói dữ liệu thành định dạng JSON chuẩn hóa, và gửi dữ liệu lên máy chủ qua kênh truyền dữ liệu hai chiều (Wi-Fi hoặc Ethernet). Ở giai đoạn hiện tại, phần xử lý nhúng được mô phỏng bằng phần mềm trên máy tính; phần cứng Raspberry Pi sẽ được triển khai khi có thiết bị thực tế.

\subsubsection{Tầng máy chủ (Server Layer)}

\hspace{0.5cm} Máy chủ được xây dựng bằng FastAPI (Python), một framework hiện đại hỗ trợ bất đồng bộ (async). Tầng này đảm nhiệm các chức năng cốt lõi của hệ thống: tiếp nhận dữ liệu cảm biến, chuyển đổi tọa độ giữa các hệ quy chiếu (WGS-84, ECEF, ENU), tính toán tọa độ mục tiêu từ vector định hướng và khoảng cách, áp dụng bộ lọc Kalman hoặc alpha-beta để ước lượng vị trí và vận tốc, quản lý phiên làm việc (session), và phát dữ liệu đã xử lý xuống giao diện qua kênh truyền dữ liệu hai chiều.

Tầng máy chủ cung cấp hai loại giao diện: REST API cho các thao tác quản lý (tạo phiên, dừng phiên, truy vấn lịch sử, xuất dữ liệu) và kênh truyền dữ liệu hai chiều cho việc truyền tải dữ liệu thời gian thực.

\subsubsection{Tầng giao diện (Frontend Layer)}

\hspace{0.5cm} Giao diện người dùng được xây dựng bằng React 19 kết hợp với Vite, sử dụng thư viện bản đồ số để hiển thị bản đồ OpenStreetMap. Các chức năng chính gồm: hiển thị bản đồ nền với vị trí người quan sát và mục tiêu, cập nhật vị trí mục tiêu theo thời gian thực, hiển thị lộ trình di chuyển của mục tiêu (quỹ đạo), hiển thị thông số đo lường (tọa độ, góc, khoảng cách, vận tốc), điều khiển phiên (bắt đầu, dừng, cấu hình tham số), và hiển thị biểu đồ sai số theo thời gian.

\subsection{Cơ sở lý thuyết mở rộng}

\hspace{0.5cm} Giai đoạn 1 đã trình bày các kiến thức nền tảng về GPS, tam giác cầu, các phép chiếu bản đồ và các thuật toán geodesic (Haversine, Vincenty, Karney). Giai đoạn 2 mở rộng thêm các khái niệm sau để phục vụ bài toán tracking thời gian thực.

\subsubsection{Hệ tọa độ Body frame và chuyển đổi}

\hspace{0.5cm} Bên cạnh các hệ tọa độ WGS-84, ECEF, ENU đã sử dụng, dự án cần thêm hệ tọa độ Body frame (hệ tọa độ gắn với thiết bị). Body frame có gốc tại trọng tâm của thiết bị, trục $x$ hướng về phía trước, trục $y$ hướng sang phải, trục $z$ hướng xuống dưới. Đây là hệ quy chiếu tự nhiên để mô tả góc ngắm azimuth và elevation từ cảm biến IMU.

Chuyển đổi từ Body frame sang ENU được thực hiện bằng ma trận xoay được xây dựng từ các góc roll ($\phi$), pitch ($\theta$), yaw ($\psi$) đo từ IMU:

\begin{equation}
\mathbf{R}_{\text{body}}^{\text{ENU}} = \mathbf{R}_z(\psi) \cdot \mathbf{R}_y(\theta) \cdot \mathbf{R}_x(\phi)
\end{equation}

Trong đó:
\begin{align}
\mathbf{R}_z(\psi) &= \begin{bmatrix} \cos\psi & -\sin\psi & 0 \\ \sin\psi & \cos\psi & 0 \\ 0 & 0 & 1 \end{bmatrix}, \quad
\mathbf{R}_y(\theta) = \begin{bmatrix} \cos\theta & 0 & \sin\theta \\ 0 & 1 & 0 \\ -\sin\theta & 0 & \cos\theta \end{bmatrix} \\
\mathbf{R}_x(\phi) &= \begin{bmatrix} 1 & 0 & 0 \\ 0 & \cos\phi & -\sin\phi \\ 0 & \sin\phi & \cos\phi \end{bmatrix}
\end{align}

\subsubsection{Kết hợp đa cảm biến (Sensor Fusion)}

\hspace{0.5cm} Việc kết hợp dữ liệu từ nhiều cảm biến là điểm khác biệt chính của giai đoạn 2 so với giai đoạn 1. Mỗi cảm biến có ưu và nhược điểm riêng:

\begin{itemize}
    \item GPS cho vị trí tuyệt đối nhưng tần số thấp (1 đến 10 Hz) và dễ mất tín hiệu trong khu vực che chắn.
    \item IMU có tần số cao (100 đến 1000 Hz) nhưng bị trôi theo thời gian, sai số tích lũy tăng dần.
    \item Laser cho khoảng cách chính xác nhưng yêu cầu tầm nhìn thẳng (LOS) và bị giới hạn tầm xa.
\end{itemize}

Kết hợp ba nguồn này cho phép ước lượng vị trí mục tiêu ngay cả khi một hoặc hai cảm biến suy giảm. Phương pháp hợp nhất cảm biến sử dụng trong dự án là mô hình lỗi tổng hợp theo phương pháp RSS (Root Sum of Squares), tính độ bất định tổng hợp $\sigma_{\text{pos}}$ từ phương sai của từng cảm biến:

\begin{equation}
\sigma_{\text{pos}} = \sqrt{\sigma_{\text{gps}}^2 + (R \cdot \sin\sigma_{\text{az}})^2 + \sigma_{\text{range}}^2 + (R \cdot \sin\sigma_{\text{el}})^2}
\end{equation}

Trong đó $R$ là khoảng cách từ người quan sát đến mục tiêu, $\sigma_{\text{gps}}$ là sai số GPS, $\sigma_{\text{az}}$ và $\sigma_{\text{el}}$ là sai số góc azimuth và elevation của IMU, $\sigma_{\text{range}}$ là sai số laser.

\subsubsection{Bộ lọc Kalman}

\hspace{0.5cm} Bộ lọc Kalman (Kalman filter) là thuật toán ước lượng trạng thái kinh điển trong điều khiển và dẫn đường, do Rudolf Kalman công bố năm 1960. Thuật toán ước lượng trạng thái của hệ thống từ các đo lường nhiễu và mô hình động học, hoạt động theo hai bước lặp: dự báo (predict) và cập nhật (update).

Minh họa bộ lọc bằng mô hình đơn giản nhất: mô hình vận tốc không đổi (Constant Velocity, CV) trong không gian 2D (East-North). Vector trạng thái bao gồm vị trí và vận tốc trên hai trục:

\begin{equation}
\mathbf{x}_k = \begin{bmatrix} e_k & n_k & \dot{e}_k & \dot{n}_k \end{bmatrix}^\top
\end{equation}

Ma trận chuyển tiếp trạng thái $\mathbf{F}$ mô hình hóa giả thiết vận tốc không đổi trong khoảng thời gian $\Delta t$:

\begin{equation}
\mathbf{F}_{2\text{D}} = \begin{bmatrix}
1 & 0 & \Delta t & 0 \\
0 & 1 & 0 & \Delta t \\
0 & 0 & 1 & 0 \\
0 & 0 & 0 & 1
\end{bmatrix}
\end{equation}

Đối với mục tiêu drone cần theo dõi cả trục Up (chiều cao), vector trạng thái mở rộng thành 6 chiều:

\begin{equation}
\mathbf{x}_k = \begin{bmatrix} e_k & n_k & u_k & \dot{e}_k & \dot{n}_k & \dot{u}_k \end{bmatrix}^\top
\end{equation}

Và ma trận $\mathbf{F}$ mở rộng tương ứng thành $6 \times 6$ (bộ lọc 3D).

\subsubsection{Ma trận nhiễu quá trình và khởi tạo}

\hspace{0.5cm} Ma trận nhiễu quá trình $\mathbf{Q}$ mô tả mức độ không chắc chắn trong mô hình động học. Dự án sử dụng mô hình DWNA (Discrete White Noise Acceleration), trong đó gia tốc được giả thiết là nhiễu trắng rời rạc:

\begin{equation}
\mathbf{Q}_{2\text{D}} = \sigma_a^2 \begin{bmatrix}
\frac{\Delta t^4}{4} & 0 & \frac{\Delta t^3}{2} & 0 \\
0 & \frac{\Delta t^4}{4} & 0 & \frac{\Delta t^3}{2} \\
\frac{\Delta t^3}{2} & 0 & \Delta t^2 & 0 \\
0 & \frac{\Delta t^3}{2} & 0 & \Delta t^2
\end{bmatrix}
\end{equation}

Trong đó $\sigma_a$ là độ lệch chuẩn gia tốc, hiệu chỉnh theo loại mục tiêu:

\begin{table}[H]
\centering
\caption{Tham số nhiễu quá trình $\sigma_a$ theo loại mục tiêu}
\label{tab:process-noise}
\adjustbox{max width=\linewidth}{
\begin{tabular}{lcc}
\toprule
\textbf{Loại mục tiêu} & $\sigma_a$ (m/s$^2$) & \textbf{Lý do} \\
\midrule
Người đi bộ & 0,5 & Di chuyển đều, đổi hướng chậm \\
Xe máy & 5,0 & Cơ động cao, phanh/accel đột ngột \\
Drone & 5,0 & Bay 3D, thay đổi hướng và độ cao \\
\bottomrule
\end{tabular}
}
\end{table}

Giá trị $\sigma_a = 0{,}5$ m/s$^2$ cho người đi bộ phản ánh gia tốc nhỏ khi di chuyển đều (bước chân khoảng 0,3 đến 0,5 m/s$^2$). Giá trị $\sigma_a = 5{,}0$ m/s$^2$ cho xe máy và drone phản ánh khả năng cơ động cao: xe máy có thể phanh hoặc tăng tốc 3 đến 8 m/s$^2$ trong đô thị; drone có thể thay đổi hướng 360$^\circ$ trong vài giây.

Giá trị khởi tạo ban đầu của ma trận hiệp phương sai trạng thái $\mathbf{P}_0$ ảnh hưởng đến tốc độ hội tụ của bộ lọc. $\mathbf{P}_0$ được thiết lập với phương sai lớn ở các trạng thái vị trí (100 m$^2$) và vận tốc (25 m$^2$/s$^2$), thể hiện sự không chắc chắn ban đầu cao:

\begin{equation}
\mathbf{P}_0 = \text{diag}(100, 100, 25, 25) \quad \text{(2D)} \qquad \mathbf{P}_0 = \text{diag}(100, 100, 100, 25, 25, 25) \quad \text{(3D)}
\end{equation}

Vector trạng thái ban đầu $\mathbf{x}_0$ được khởi tạo bằng phép đo đầu tiên từ hợp nhất cảm biến, không sử dụng giá trị bằng không để tránh lag hội tụ không cần thiết.

\subsubsection{Bộ lọc alpha-beta}

\hspace{0.5cm} Bộ lọc alpha-beta (còn gọi là g-h filter) là phiên bản đơn giản hóa của bộ lọc Kalman, chỉ dùng hai tham số: $\alpha$ cho vị trí và $\beta$ cho vận tốc. Bộ lọc này phù hợp với các hệ thống có tài nguyên tính toán hạn chế và vẫn cho kết quả tốt với các mục tiêu di chuyển đều.

\begin{equation}
\begin{aligned}
x_{k}^{p} &= x_{k-1} + \Delta t \cdot \dot{x}_{k-1} \quad \text{(Dự báo)} \\
x_{k} &= x_{k}^{p} + \alpha (z_{k} - x_{k}^{p}) \quad \text{(Cập nhật vị trí)} \\
\dot{x}_{k} &= \dot{x}_{k-1} + \frac{\beta}{\Delta t} (z_{k} - x_{k}^{p}) \quad \text{(Cập nhật vận tốc)}
\end{aligned}
\end{equation}

Trong đó $x_k^p$ là giá trị dự báo, $z_k$ là phép đo, $\alpha$ và $\beta$ là tham số lọc. Giá trị $\beta$ được chọn theo điều kiện critically damped (đáp tuyến crititcal) của Benedict-Bordner:

\begin{equation}
\beta = (2 - \alpha) - 2\sqrt{1 - \alpha}
\end{equation}

Với $\alpha = 0{,}4$ (giá trị mặc định), ta có $\beta \approx 0{,}051$. Bộ lọc critically damped không bị overshoot khi mục tiêu đổi hướng đột ngột, phù hợp cho bài toán tracking.

\subsection{Phân tích yêu cầu hiệu suất}

\hspace{0.5cm} Từ các ràng buộc của bài toán, nhóm xác định các chỉ số hiệu suất chính mà hệ thống phải đạt được:

\begin{table}[H]
\centering
\caption{Các chỉ số hiệu suất thiết kế của hệ thống}
\label{tab:performance-spec}
\adjustbox{max width=\linewidth}{
\begin{tabular}{lll}
\toprule
\textbf{Chỉ số} & \textbf{Giá trị yêu cầu} & \textbf{Lý do} \\
\midrule
Độ trễ xử lý chu kỳ & $<$ 100 ms & Đảm bảo 10 Hz real-time \\
RMSE vị trí & $<$ 5,0 m & Chính xác theo dõi trong 500 m \\
Tần số cập nhật & 10 Hz & Mượt mà trên bản đồ \\
Phiên đồng thời & $\geq$ 1 & Nhiều thiết bị theo dõi \\
Thời gian khởi tạo & $<$ 2 s & Phản hồi nhanh khi bắt đầu \\
Biên hoạt động & 100 đến 1000 m & Phạm vi cảm biến thực tế \\
\bottomrule
\end{tabular}
}
\end{table}

\subsubsection{Phân tích thời gian xử lý}

\hspace{0.5cm} Chu kỳ xử lý 10 Hz yêu cầu tổng thời gian xử lý mỗi khung không vượt quá 100 ms. Phân tích ban đầu dự kiến phân bổ thời gian như sau:

\begin{itemize}
    \item \textbf{Chuyển đổi tọa độ}: WGS-84 $\rightarrow$ ECEF $\rightarrow$ ENU và ngược lại. Thời gian dự kiến: $<$ 10 $\mu$s.
    \item \textbf{Hợp nhất cảm biến}: Tính $\sigma_{\text{pos}}$ từ RSS. Thời gian dự kiến: $<$ 5 $\mu$s.
    \item \textbf{Bộ lọc Kalman}: Predict + Update. Thời gian dự kiến: $<$ 50 $\mu$s.
    \item \textbf{Chuyển đổi ngược}: ENU $\rightarrow$ WGS-84. Thời gian dự kiến: $<$ 10 $\mu$s.
    \item \textbf{Tổng thời gian tính toán}: $<$ 100 $\mu$s, chiếm 0,1\% chu kỳ 100 ms.
    \item \textbf{Truyền tải kênh truyền dữ liệu hai chiều}: $<$ 10 ms trên mạng LAN.
    \item \textbf{Hiển thị giao diện}: $<$ 10 ms với thư viện bản đồ số.
\end{itemize}

Tổng thời gian xử lý dự kiến nhỏ hơn 25 ms, để lại biên an toàn lớn cho việc mở rộng phiên đồng thời và xử lý ngoại lệ.

\subsubsection{Phân tích độ chính xác}

\hspace{0.5cm} Tiêu chuẩn thiết kế yêu cầu RMSE nhỏ hơn 5,0 m trong phạm vi 500 m. Từ mô hình lỗi RSS (Root Sum of Squares), sai số tổng hợp $\sigma_{\text{pos}}$ tại các khoảng cách điển hình:

\begin{itemize}
    \item \textbf{Khoảng cách 50 m}: $\sigma_{\text{pos}} \approx 5{,}0$ m (GPS chiếm 98\% tổng sai số).
    \item \textbf{Khoảng cách 500 m}: $\sigma_{\text{pos}} \approx 5{,}9$ m (IMU đóng góp 28\%).
    \item \textbf{Khoảng cách 1000 m}: $\sigma_{\text{pos}} \approx 8{,}1$ m (IMU đóng góp 61\%).
\end{itemize}

Điểm giao nhau (cross-over range) xảy ra tại khoảng 794 m, nơi đóng góp sai số từ GPS bằng đóng góp từ IMU. Dưới ngưỡng này, GPS là yếu tố quyết định; trên ngưỡng này, IMU là yếu tố chi phối. Bộ lọc Kalman với cơ chế Adaptive R sẽ tự động điều chỉnh trọng số đo lường theo khoảng cách thực tế.

\subsection{So sánh bộ lọc Kalman và Alpha-Beta}

\hspace{0.5cm} Hệ thống triển khai cả hai bộ lọc (Kalman và alpha-beta) để so sánh hiệu suất và phục vụ theo yêu cầu so sánh. Bảng \ref{tab:filter-compare-w1} tóm tắt đặc điểm của hai bộ lọc.

\begin{table}[H]
\centering
\caption{So sánh đặc điểm hai bộ lọc được triển khai}
\label{tab:filter-compare-w1}
\adjustbox{max width=\linewidth}{
\begin{tabular}{p{3.5cm}p{5cm}p{5cm}}
\toprule
\textbf{Đặc điểm} & \textbf{Bộ lọc Kalman} & \textbf{Bộ lọc Alpha-Beta} \\
\midrule
Tham số & Tự động qua $\mathbf{K}_k$ & $\alpha$, $\beta$ cố định \\
Bất định & Có ($\mathbf{P}_k$, uncertainty\_m) & Không \\
Độ phức tạp & $\mathcal{O}(n^3)$ với $n$ là số trạng thái & $\mathcal{O}(n)$ \\
Thời gian tính & $\sim$48,5 $\mu$s/bước (2D) & $\sim$10 $\mu$s/bước \\
Thực hiện 3D & Có (6 trạng thái, theo drone) & Chỉ East-North (2D) \\
Phù hợp & Mục tiêu cơ động, nhiễu thay đổi & Mục tiêu di chuyển đều, tài nguyên hạn chế \\
\bottomrule
\end{tabular}
}
\end{table}

Bộ lọc Kalman có ưu điểm về tính thích nghi và khả năng ước lượng độ bất định (qua ma trận hiệp phương sai $\mathbf{P}_k$). Thông tin bất định này quan trọng trong các ứng dụng quân sự hoặc dân sự cần biết mức độ tin tưởng vào ước lượng vị trí. Tuy nhiên, bộ lọc Kalman đòi hỏi thời gian tính toán lớn hơn và cần giai đoạn hội tụ ban đầu.

Bộ lọc alpha-beta có ưu điểm về tốc độ và độ đơn giản. Với tham số $\alpha$ và $\beta$ cố định, bộ lọc không cần quá trình hội tụ và hoạt động ổn định ngay từ bước đầu tiên. Trên Raspberry Pi, tài nguyên tiết kiệm được có thể dùng cho các tác vụ khác như đọc cảm biến và giao tiếp mạng.

\subsection{Sơ đồ use-case}

\hspace{0.5cm} Hình \ref{fig:usecase-w1} mô tả sơ đồ use-case của hệ thống tracking mục tiêu. Người quan sát (actor chính) có thể thực hiện các hành động: khởi tạo phiên tracking, cấu hình tham số (loại mục tiêu, thuật toán lọc, alpha, thời gian, bán kính biên), bắt đầu và dừng phiên, xem lộ trình di chuyển, và xuất dữ liệu CSV.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{Images/use-case-diagram.png}
\caption{Sơ đồ use-case của hệ thống tracking mục tiêu di động}
\label{fig:usecase-w1}
\end{figure}

Hệ thống tự động thực hiện các tác vụ nền: cập nhật dữ liệu cảm biến theo chu kỳ 10 Hz, tính toán tọa độ mục tiêu, áp dụng bộ lọc ước lượng, truyền tải kết quả qua kênh truyền dữ liệu hai chiều, hiển thị vị trí và quỹ đạo trên bản đồ, và cảnh báo khi mất tín hiệu cảm biến.

\subsection{Luồng xử lý dữ liệu tổng quát}

\hspace{0.5cm} Toàn bộ luồng xử lý dữ liệu trong hệ thống Phase 2 được mô tả theo các bước sau:

\begin{enumerate}
    \item \textbf{Đọc cảm biến}: Raspberry Pi (hoặc phần mềm mô phỏng) đọc dữ liệu từ ba cảm biến (GPS, IMU, laser) theo chu kỳ đều đặn 10 Hz (mỗi 100 ms).
    \item \textbf{Chuẩn hóa dữ liệu}: GPS chuyển sang tọa độ thập phân, IMU tính góc hướng từ giá trị gyro/accelerometer, laser lấy giá trị khoảng cách đơn.
    \item \textbf{Đóng gói JSON}: Bộ ba dữ liệu GPS, IMU, laser được đóng gói thành một bản tin JSON chuẩn hóa với timestamp.
    \item \textbf{Truyền tải}: Bản tin JSON được gửi lên máy chủ qua kênh truyền dữ liệu hai chiều.
    \item \textbf{Xử lý phía server}: Máy chủ nhận dữ liệu và thực hiện năm bước: (a) chuyển đổi GPS từ WGS-84 sang ECEF rồi sang ENU, (b) tạo vector định hướng 3D từ azimuth và elevation, (c) nhân vector với khoảng cách laser để thu được tọa độ tương đối, (d) cộng vào vị trí người quan sát để thu được tọa độ mục tiêu trong ENU, (e) áp dụng bộ lọc Kalman hoặc alpha-beta để ước lượng.
    \item \textbf{Phát kết quả}: Server gửi tọa độ mục tiêu đã xử lý xuống giao diện qua kênh truyền dữ liệu hai chiều.
    \item \textbf{Hiển thị}: Giao diện cập nhật vị trí mục tiêu trên bản đồ, vẽ quỹ đạo, cập nhật bảng số liệu.
\end{enumerate}

\subsection{Chuyển đổi tọa độ trong hệ thống}

\hspace{0.5cm} Chuỗi chuyển đổi tọa độ là khâu bắt buộc của hệ thống. Đầu vào là tọa độ WGS-84 của người quan sát. Đầu ra là tọa độ WGS-84 của mục tiêu. Ở giữa, cần chuyển qua nhiều hệ trung gian:

\begin{equation}
\begin{aligned}
\text{WGS-84} &\xrightarrow{(1)} \text{ECEF} \\
\text{ECEF} &\xrightarrow{(2)} \text{ENU (gốc tại người quan sát)} \\
\text{ENU} &\xrightarrow{(3)} \text{Cộng vector định hướng} \times \text{khoảng cách} \\
\text{Kết quả} &\xrightarrow{(4)} \text{WGS-84}
\end{aligned}
\end{equation}

Bước (1) sử dụng công thức chuyển đổi ellipsoid từ WGS-84 sang ECEF (Earth-Centered, Earth-Fixed). Bước (2) sử dụng ma trận xoay $\mathbf{R}_{ecef}^{enu}$ được xây dựng từ kinh độ và vĩ độ của người quan sát. Bước (3) là phép tính vector cộng:

\begin{equation}
\mathbf{p}_{\text{target}}^{\text{ENU}} = \mathbf{p}_{\text{observer}}^{\text{ENU}} + d \cdot \mathbf{v}(\alpha, \beta)
\end{equation}

Trong đó $d$ là khoảng cách laser, $\alpha$ là azimuth, $\beta$ là elevation, $\mathbf{v}$ là vector đơn vị hướng.

Hình \ref{fig:coord-flow-w1} mô tả chuỗi chuyển đổi tọa độ.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{Images/coord-flow.png}
\caption{Chuỗi chuyển đổi tọa độ LLA-ECEF-ENU trong hệ thống}
\label{fig:coord-flow-w1}
\end{figure}

\subsubsection{Ví dụ tính toán số liệu}

\hspace{0.5cm} Để minh họa rõ ràng chuỗi chuyển đổi, xét ví dụ sau: người quan sát đứng tại tọa độ GPS ($\phi = 10{,}7726^\circ$ N, $\lambda = 106{,}6983^\circ$ E, $h = 10$ m), hướng azimuth $\alpha = 45^\circ$, elevation $\beta = 5^\circ$, khoảng cách laser $d = 200$ m.

\textbf{Bước 1: WGS-84 sang ECEF.} Sử dụng ellipsoid WGS-84 với bán kính trục lớn $a = 6\,378\,137{,}0$ m và bán kính trục nhỏ $b = 6\,356\,752{,}314\,245$ m:

\begin{align}
N(\phi) &= \frac{a}{\sqrt{1 - e^2 \sin^2\phi}} = \frac{6\,378\,137}{\sqrt{1 - 0{,}006\,694 \cdot \sin^2(10{,}7726^\circ)}} \approx 6\,378\,137 \text{ m} \\
X_{\text{obs}} &= (N + h) \cos\phi \cos\lambda = (6\,378\,137 + 10) \cdot \cos(10{,}7726^\circ) \cdot \cos(106{,}6983^\circ) \notag \\
&\approx 5\,753\,842 \text{ m} \\
Y_{\text{obs}} &= (N + h) \cos\phi \sin\lambda \approx 2\,675\,415 \text{ m} \\
Z_{\text{obs}} &= N(1 - e^2) \sin\phi \approx 1\,197\,893 \text{ m}
\end{align}

\textbf{Bước 2: ECEF sang ENU.} Tọa độ ENU có gốc tại người quan sát. Ma trận xoay $\mathbf{R}_{ecef}^{enu}$ chuyển vector $\Delta\mathbf{p} = \mathbf{p}_{\text{target}} - \mathbf{p}_{\text{obs}}$ từ ECEF sang ENU.

\textbf{Bước 3: Tính tọa độ tương đối từ laser.} Vector đơn vị hướng từ góc ngắm:

\begin{align}
v_e &= \cos\beta \cdot \sin\alpha = \cos(5^\circ) \cdot \sin(45^\circ) \approx 0{,}703 \\
v_n &= \cos\beta \cdot \cos\alpha = \cos(5^\circ) \cdot \cos(45^\circ) \approx 0{,}703 \\
v_u &= \sin\beta = \sin(5^\circ) \approx 0{,}087
\end{align}

Tọa độ ENU của mục tiêu so với người quan sát:

\begin{align}
\Delta e &= d \cdot v_e = 200 \cdot 0{,}703 \approx 140{,}7 \text{ m} \\
\Delta n &= d \cdot v_n = 200 \cdot 0{,}703 \approx 140{,}7 \text{ m} \\
\Delta u &= d \cdot v_u = 200 \cdot 0{,}087 \approx 17{,}5 \text{ m}
\end{align}

\textbf{Bước 4: ENU sang WGS-84.} Cộng tọa độ tương đối vào vị trí người quan sát, sau đó chuyển ngược về WGS-84 bằng công thức inversion. Kết quả: mục tiêu nằm cách người quan sát khoảng 200 m theo hướng Đông-Bắc, cao hơn 17,5 m.

Ví dụ trên cho thấy mỗi bước chuyển đổi đều bảo toàn chính xác số học và không có phép tính xấp xỉ đáng kể. Sai số chủ yếu đến từ nhiễu cảm biến (GPS, IMU, laser), không phải từ sai số số học.

\subsection{Kịch bản mô phỏng}

\hspace{0.5cm} Để đánh giá hệ thống trước khi có dữ liệu cảm biến thực tế, nhóm triển khai kịch bản mô phỏng với các thông số được chọn có cơ sở lý thuyết và thực tiễn rõ ràng.

\subsubsection{Giải thích tham số mô phỏng}

\hspace{0.5cm} Mỗi tham số mô phỏng đều có lý do cụ thể:

\begin{table}[H]
\centering
\caption{Tham số mô phỏng và cơ sở lựa chọn}
\label{tab:sim-params}
\begin{tabularx}{\linewidth}{lX}
\toprule
\textbf{Tham số} & \textbf{Cơ sở lựa chọn} \\
\midrule
Seed = 42 & Seed cố định đảm bảo tính tái lập: mọi lần chạy đều cho cùng kết quả RMSE, giúp so sánh thuật toán một cách công bằng. Giá trị 42 là lựa chọn phổ biến trong nghiên cứu (được nhiều bộ benchmark sử dụng). \\
Thời gian 120 s & 120 giây tương đương 1.200 bước thời gian ở tần số 10 Hz. Đây là khoảng đủ dài để bộ lọc Kalman hội tụ (khoảng 15 đến 20 giây đầu) và thu thập đủ dữ liệu thống kê về RMSE. Thời gian ngắn hơn (30 s) không đủ hội tụ; thời gian dài hơn (300 s) không cải thiện RMSE đáng kể nhưng tăng thời gian mô phỏng. \\
Tần số 10 Hz & Tần số 10 Hz phù hợp với tần số cập nhật của GPS dân sự (1 đến 10 Hz) và laser rangefinder (10 đến 100 Hz). Sau nội suy, cả ba cảm biến đều được đồng bộ về cùng một lưới thời gian 10 Hz. Tần số cao hơn (50 Hz) tăng chi phí tính toán mà không cải thiện RMSE đáng kể; tần số thấp hơn (1 Hz) mất thông tin động học của mục tiêu. \\
Biên 400 m & Bán kính 400 m nằm trong khoảng hiệu dụng của laser rangefinder (100 đến 1000 m) và đảm bảo mục tiêu nằm trong vùng laser luôn chính xác hơn GPS (dưới ngưỡng cắt ngang 794 m). Bán kính lớn hơn (800 m) sẽ đưa một số vị trí ra ngoài vùng laser ưu thế; nhỏ hơn (200 m) giới hạn phạm vi thực tế quá hẹp. \\
\bottomrule
\end{tabularx}
\end{table}

\subsubsection{Vấn đề không đồng bộ tần số cảm biến}

\hspace{0.5cm} Trong hệ thống thực tế, ba cảm biến hoạt động ở tần số khác nhau: GPS 1 đến 10 Hz, IMU 100 đến 1000 Hz, laser 10 đến 100 Hz. Để hợp nhất dữ liệu, tất cả phải được nội suy về cùng một lưới thời gian chung. Dự án sử dụng nội suy PCHIP (Piecewise Cubic Hermite Interpolating Polynomial) để giữ dạng sóng của tín hiệu, tốt hơn nội suy tuyến tính hoặc spline ba bậc trong việc tránh overshoot.

Khi khoảng cách giữa hai mẫu thời gian liền kề quá lớn (do mất tín hiệu cảm biến), bộ lọc Kalman tự động dự báo (predict) mà không cập nhật (update), duy trì ước lượng dựa trên mô hình động học. Cơ chế này giúp hệ thống chịu được mất tín hiệu tạm thời lên đến 2 đến 3 giây.

\subsubsection{Xử lý lỗi tương quan giữa cảm biến}

\hspace{0.5cm} Trong mô hình RSS chuẩn, các thành phần sai số được giả thiết độc lập (GPS, IMU, laser không bị lỗi đồng thời). Tuy nhiên, trong thực tế, một số nguồn lỗi có thể tương quan: GPS multipath và IMU drift đều phụ thuộc vào môi trường che chắn. Hiện tại, ma trận R (nhiễu đo lường) được thiết lập dạng chéo (diagonal), chỉ chứa phương sai của từng cảm biến mà không có thành phần hiệp phương sai. Khi có dữ liệu thực, ma trận R có thể được mở rộng bằng thêm các thành phần hiệp phương sai để mô tả tương quan.

\subsubsection{Chi tiết kịch bản mô phỏng}

\hspace{0.5cm} Ba loại mục tiêu được mô phỏng tương ứng với ba trường hợp sử dụng thực tế:

\begin{enumerate}
    \item \textbf{Người đi bộ}: Tốc độ 1 đến 2 m/s, quỹ đạo ngẫu nhiên trên mặt phẳng. Sử dụng bộ lọc Kalman 2D (4 trạng thái).
    \item \textbf{Xe máy}: Tốc độ 5 đến 15 m/s, quỹ đạo với thay đổi hướng và tốc độ đột ngột. Sử dụng bộ lọc Kalman 2D (4 trạng thái).
    \item \textbf{Drone}: Tốc độ 5 đến 15 m/s, bay 3D với độ cao thay đổi. Sử dụng bộ lọc Kalman 3D (6 trạng thái).
\end{enumerate}

\subsection{Kết quả kiểm chứng ban đầu}

\hspace{0.5cm} Kết quả mô phỏng với giá trị khởi tạo cố định (seed = 42) cho thấy hệ thống đạt yêu cầu thiết kế trên cả ba kịch bản:

\begin{table}[H]
\centering
\caption{RMSE ngang của ba bộ lọc trên ba kịch bản mục tiêu (seed = 42)}
\label{tab:rmse-w1}
\adjustbox{max width=\linewidth}{
\begin{tabular}{lcccc}
\toprule
\textbf{Kịch bản} & \textbf{Đo thô} & \textbf{Alpha-beta} & \textbf{Kalman} & \textbf{Yêu cầu} \\
\midrule
Người đi bộ & 0,48 m & \textbf{0,26 m} & 0,86 m & $<$ 5 m \checkmark \\
Xe máy      & 1,89 m & \textbf{1,02 m} & 1,80 m & $<$ 5 m \checkmark \\
Drone       & 1,94 m & \textbf{1,06 m} & 2,10 m & $<$ 5 m \checkmark \\
\bottomrule
\end{tabular}
}
\end{table}

Cả ba kịch bản đều đạt yêu cầu RMSE $<$ 5,0 m. Bộ lọc alpha-beta đạt RMSE thấp hơn bộ lọc Kalman ở cả ba trường hợp, nguyên nhân là mô phỏng sử dụng mô hình vận tốc không đổi (CV), phù hợp với thiết kế của alpha-beta. Tuy nhiên, bộ lọc Kalman cung cấp thông tin bất định (ma trận $\mathbf{P}_k$) mà alpha-beta không có.

Hình \ref{fig:rmse-bar-w1} biểu diễn so sánh trực quan RMSE giữa ba phương pháp.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/rmse_comparison_bar.png}
\caption{So sánh RMSE ngang giữa ba phương pháp trên ba kịch bản mục tiêu}
\label{fig:rmse-bar-w1}
\end{figure}

\subsection{Phân tích kết quả đa seed}

\hspace{0.5cm} Kết quả ở seed = 42 chỉ phản ánh một kịch bản khởi tạo duy nhất. Để đánh giá độ ổn định thống kê, nhóm chạy mô phỏng với 10 seed khác nhau (seed = 1 đến 10), mỗi seed tạo một quỹ đạo mục tiêu và nhiễu cảm biến riêng biệt. Bảng \ref{tab:multiseed-w1} tổng hợp kết quả trung bình và độ lệch chuẩn.

\begin{table}[H]
\centering
\caption{Kết quả RMSE đa seed (10 seed, 120 s, 10 Hz, 400 m)}
\label{tab:multiseed-w1}
\adjustbox{max width=\linewidth}{
\begin{tabular}{llcccc}
\toprule
\textbf{Kịch bản} & \textbf{Phương pháp} & \textbf{TB (m)} & \textbf{ĐLC (m)} & \textbf{Min (m)} & \textbf{Max (m)} \\
\midrule
\multirow{2}{*}{Người đi bộ} & Alpha-beta & 0,34 & 0,08 & 0,22 & 0,45 \\
& Kalman & 0,92 & 0,22 & 0,57 & 1,24 \\
\midrule
\multirow{2}{*}{Xe máy} & Alpha-beta & 0,91 & 0,08 & 0,78 & 1,02 \\
& Kalman & 1,53 & 0,22 & 1,27 & 1,83 \\
\midrule
\multirow{2}{*}{Drone} & Alpha-beta & 1,09 & 0,23 & 0,71 & 1,50 \\
& Kalman & 2,39 & 0,32 & 1,82 & 2,83 \\
\bottomrule
\end{tabular}
}
\end{table}

Kết quả đa seed cho thấy: (a) bộ lọc alpha-beta có độ ổn định cao hơn Kalman qua các seed, với độ lệch chuẩn nhỏ hơn ở cả ba kịch bản; (b) sai số Kalman dao động rộng hơn do phụ thuộc vào quá trình hội tụ ban đầu, nhưng vẫn luôn dưới ngưỡng 5,0 m; (c) kịch bản drone có sai số cao nhất do độ phức tạp của bài toán 3 chiều.

\subsection{Phân tích ngưỡng cắt ngang laser}

\hspace{0.5cm} Trong hệ thống thực tế, laser rangefinder có giới hạn tầm xa. Khi khoảng cách từ người quan sát đến mục tiêu vượt quá ngưỡng nào đó, đóng góp sai số từ laser vượt quá đóng góp từ GPS. Điểm giao nhau được tính khi $\sigma_{\text{gps}}^2 = (R \cdot \sin\sigma_{\text{az}})^2 + \sigma_{\text{range}}^2 + (R \cdot \sin\sigma_{\text{el}})^2$.

Với tham số mặc định ($\sigma_{\text{gps}} = 5{,}0$ m, $\sigma_{\text{az}} = 0{,}3^\circ$, $\sigma_{\text{el}} = 0{,}2^\circ$, $\sigma_{\text{range}} = 0{,}5$ m), ngưỡng cắt ngang xảy ra tại khoảng \textbf{794 m}. Hình \ref{fig:crossover-w1} minh họa sự chuyển đổi ưu thế giữa hai nguồn sai số.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{Images/crossover_plot.png}
\caption{Phân tích ngưỡng cắt ngang giữa GPS và laser: dưới 794 m, GPS chiếm ưu thế; trên 794 m, laser chiếm ưu thế}
\label{fig:crossover-w1}
\end{figure}

Đối với vùng hoạt động thực tế (bán kính 400 m), laser luôn là nguồn chính xác hơn GPS. Điều này có nghĩa là trong hầu hết các kịch bản sử dụng, hợp nhất cảm biến sẽ ưu tiên thông tin laser hơn GPS. Bộ lọc Kalman với cơ chế Adaptive R tự động điều chỉnh trọng số đo lường phù hợp.

\subsection{Phân tích đường cong sai số theo thời gian}

\hspace{0.5cm} Hình \ref{fig:error-ts-ped} thể hiện diễn biến sai số vị trí theo thời gian cho kịch bản người đi bộ. Ba đường màu thể hiện sai số tức thời của từng phương pháp: đo thô (màu cam), alpha-beta (màu xanh dương) và Kalman (màu đỏ).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/error_ts_pedestrian-kf_ab-synthetic.png}
\caption{Sai số vị trí theo thời gian cho kịch bản người đi bộ (KF+AB, synthetic, 120 s)}
\label{fig:error-ts-ped}
\end{figure}

Đường sai số alpha-beta nhanh chóng hội tụ về giá trị ổn định thấp (khoảng 0,2 đến 0,3 m). Đường sai số Kalman có giai đoạn hội tụ ban đầu kéo dài hơn (khoảng 15 đến 20 giây đầu), sau đó cũng hội tụ về mức tương đương alpha-beta. Đường đo thô dao động mạnh xung quanh giá trị trung bình khoảng 0,48 m.

Hình \ref{fig:error-cdf-w1} thể hiện phân phối xác suất tích lũy (CDF) của sai số. Đường CDF càng dốc thì sai số càng tập trung ở mức thấp. Đường alpha-beta đạt 90\% ở khoảng 0,35 m, trong khi Kalman đạt 90\% ở khoảng 1,1 m.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{Images/error_cdf.png}
\caption{Phân phối xác suất tích lũy (CDF) sai số vị trí cho kịch bản người đi bộ}
\label{fig:error-cdf-w1}
\end{figure}

\subsection{Phân tích hiệu năng chuỗi xử lý}

\hspace{0.5cm} Hiệu năng của chuỗi xử lý (pipeline) là yếu tố quyết định khả năng hoạt động thời gian thực. Hình \ref{fig:pipeline-w1} phân tích thời gian xử lý từng bước trong chu kỳ 10 Hz.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{Images/pipeline_timing.png}
\caption{Phân tích thời gian xử lý chuỗi: tổng 59 $\mu$s, Kalman chiếm 82,2\% (48,5 $\mu$s), ECEF-ENE chiếm 6,4\% (3,8 $\mu$s)}
\label{fig:pipeline-w1}
\end{figure}

Tổng thời gian xử lý trung bình là 59 $\mu$s, nhỏ hơn nhiều so với ngưỡng 100 ms (chu kỳ 10 Hz). Bộ lọc Kalman chiếm phần lớn thời gian (82,2\%) do độ phức tạp $\mathcal{O}(n^3)$ với ma trận nghịch đảo. Chuyển đổi tọa độ ECEF-ENE chỉ chiếm 6,4\% do sử dụng các phép tính đại số tuyến tính đơn giản.

Kết quả này xác nhận rằng hệ thống có đủ biên an toàn để xử lý đồng thời nhiều phiên và mở rộng thêm các chức năng như hiển thị biểu đồ thời gian thực.

\subsection{Phân tích sai số theo loại mục tiêu}

\hspace{0.5cm} Ngoài kịch bản người đi bộ, hệ thống được kiểm tra trên hai kịch bản còn lại. Hình \ref{fig:error-ts-moto} thể hiện diễn biến sai số cho xe máy, và hình \ref{fig:altitude-drone} thể hiện độ cao drone theo thời gian.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/error_ts_motorcycle-kf_ab-synthetic.png}
\caption{Sai số vị trí theo thời gian cho kịch bản xe máy (KF+AB, synthetic, 120 s)}
\label{fig:error-ts-moto}
\end{figure}

Sai số xe máy cao hơn người đi bộ đáng kể: đo thô trung bình 1,89 m, do tốc độ cao hơn (5 đến 15 m/s so với 1 đến 2 m/s) và nhiễu lớn hơn. Bộ lọc alpha-beta đưa sai số về 1,02 m, giảm 46\% so với đo thô. Bộ lọc Kalman cho 1,80 m, giảm chỉ 5\%. Nguyên nhân là mô hình vận tốc không đổi (CV) của Kalman không bắt kịp được các thay đổi hướng đột ngột của xe máy trong đô thị.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/altitude_drone-kf_ab-synthetic.png}
\caption{Độ cao drone theo thời gian: giá trị thực (xanh lá), ước lượng Kalman (đỏ), showErrorBars=false}
\label{fig:altitude-drone}
\end{figure}

Drone là kịch bản phức tạp nhất vì phải theo dõi cả ba trục không gian. Độ cao drone thay đổi liên tục trong khoảng 20 đến 80 m, với tốc độ lên xuống khoảng 2 đến 3 m/s. Bộ lọc Kalman 3D (6 trạng thái) theo dõi được quỹ đạo 3D nhưng sai số cao hơn hai kịch bản 2D: RMSE 2,10 m so với 0,86 m (người đi bộ) và 1,80 m (xe máy).

\subsection{Phân tích ranh giới mô phỏng}

\hspace{0.5cm} Hệ thống sử dụng cơ chế ranh giới mềm (soft boundary) để giữ mục tiêu trong vùng hoạt động. Thay vì phản xạ cứng (hard reflection) tạo dao động hình bi-a, trường thế năng đẩy mục tiêu ra xa ranh giới khi khoảng cách tới ranh giới nhỏ hơn 20\% bán kính vùng:

\begin{equation}
\mathbf{F}_{\text{repulsion}} = k \cdot \max\left(0, \frac{r_{\text{boundary}} - r_{\text{target}}}{0{,}2 \cdot r_{\text{boundary}}}\right) \cdot (-\hat{\mathbf{r}})
\end{equation}

Trong đó $r_{\text{target}}$ là khoảng cách từ gốc đến mục tiêu, $r_{\text{boundary}}$ là bán kính vùng, $\hat{\mathbf{r}}$ là vector đơn vị hướng tâm, $k$ là hệ số lực đẩy. Thiết kế này tạo hành vi tự nhiên hơn phản xạ cứng, mục tiêu quay đầu dần trước khi chạm ranh giới thay vì đột ngột.

\subsection{So sánh chế độ mô phỏng tổng hợp và dữ liệu thực}

\hspace{0.5cm} Hệ thống hỗ trợ hai chế độ tạo quỹ đạo: mô phỏng tổng hợp (Synthetic) và dữ liệu thực (Real-World Data). Bảng \ref{tab:sim-modes} so sánh hai chế độ.

\begin{table}[H]
\centering
\caption{So sánh hai chế độ tạo quỹ đạo}
\label{tab:sim-modes}
\adjustbox{max width=\linewidth}{
\begin{tabular}{p{3cm}p{5cm}p{5cm}}
\toprule
\textbf{Phương diện} & \textbf{Tổng hợp (Synthetic)} & \textbf{Dữ liệu thực (Real-World)} \\
\midrule
Nguồn quỹ đạo & Mô hình toán CV ngẫu nhiên & Tập dữ liệu Geolife (đi bộ), đường bộ HCMC (xe máy) \\
Nhiễu cảm biến & Mô hình Gaussian cố định & Mô hình theo đặc tính cảm biến thực \\
Điểm khởi tạo & Ngẫu nhiên trong vùng & Chọn ngẫu nhiên từ dữ liệu \\
Đánh giá & Kiểm chuẩn thuật toán & Kiểm chứng tính thực tế \\
\bottomrule
\end{tabular}
}
\end{table}

Chế độ tổng hợp phù hợp để kiểm chuẩn thuật toán vì quỹ đạo được tạo theo mô hình toán rõ ràng, giúp so sánh công bằng giữa các phương pháp lọc. Chế độ dữ liệu thực dùng tập Geolife (252 đoạn đi bộ đã lọc từ 6.460 nhãn) và mạng lưới đường bộ thành phố Hồ Chí Minh (337.074 nút, 305.008 cạnh) để kiểm chứng tính thực tế của hệ thống.

\subsection{Bảng tổng hợp tham số cảm biến}

\hspace{0.5cm} Bảng \ref{tab:sensor-params} tổng hợp các thông số cảm biến được sử dụng trong mô phỏng, bao gồm sai số từng loại và tần số hoạt động.

\begin{table}[H]
\centering
\caption{Tham số cảm biến mô phỏng}
\label{tab:sensor-params}
\adjustbox{max width=\linewidth}{
\begin{tabular}{llcc}
\toprule
\textbf{Cảm biến} & \textbf{Tham số} & \textbf{Giá trị} & \textbf{Đơn vị} \\
\midrule
\multirow{3}{*}{GPS} & $\sigma_{\text{gps}}$ & 5,0 & m \\
& Tần số & 1 đến 10 & Hz \\
& Loại lỗi & Multipath, ionospheric & - \\
\midrule
\multirow{3}{*}{IMU} & $\sigma_{\text{az}}$ & 0,3 & độ \\
& $\sigma_{\text{el}}$ & 0,2 & độ \\
& Tần số & 100 đến 1000 & Hz \\
\midrule
\multirow{3}{*}{Laser} & $\sigma_{\text{range}}$ & 0,5 & m \\
& Tần số & 10 đến 100 & Hz \\
& Tầm hiệu dụng & 100 đến 1000 & m \\
\bottomrule
\end{tabular}
}
\end{table}

Giá trị $\sigma_{\text{gps}} = 5{,}0$ m phản ánh sai số GPS dân sự tiêu chuẩn trong điều kiện ngoài trời tốt. Giá trị $\sigma_{\text{az}} = 0{,}3^\circ$ và $\sigma_{\text{el}} = 0{,}2^\circ$ phản ánh sai số góc IMU sau khi hiệu chuẩn. Giá trị $\sigma_{\text{range}} = 0{,}5$ m phản ánh sai số laser ở khoảng cách trung bình 200 đến 400 m.

\subsection{Tóm tắt}

\hspace{0.5cm} Tuần 1 thiết kế hệ thống bám mục tiêu di động thời gian thực theo hướng hợp nhất cảm biến laser, IMU và GNSS trên kiến trúc bốn tầng: cảm biến, nhúng, máy chủ và giao diện. Bài toán được phát biểu đầy đủ ở dạng hai chiều cho người đi bộ và xe máy, dạng ba chiều cho drone, với ngưỡng sai số thiết kế dưới 5,0 m. Phần cơ sở lý thuyết gồm chuỗi chuyển đổi tọa độ địa lý sang hệ tọa độ địa phương ENU, mô hình hợp nhất cảm biến theo trọng số nghịch đảo phương sai, và hai bộ lọc bám quỹ đạo là Kalman và alpha-beta. Kết quả kiểm chứng trên kịch bản mô phỏng chuẩn (seed = 42, thời lượng 120 giây, tần suất 10 Hz, bán kính vùng hoạt động 400 m) ghi nhận sai số RMSE đo thô 0,48 m cho người đi bộ, 1,89 m cho xe máy và 1,94 m cho drone; sau lọc, bộ lọc alpha-beta đưa các giá trị tương ứng về 0,26 m, 1,02 m và 1,06 m, bộ lọc Kalman về 0,86 m, 1,80 m và 2,10 m. Cả hai phương pháp đều thỏa ngưỡng thiết kế, trong khi alpha-beta cho sai số thấp hơn trong các kịch bản mô phỏng đã chạy.
